<div class="min-h-screen w-full flex flex-col items-center p-6 pt-24">
  <div class="w-full max-w-2xl bg-gray-900 bg-opacity-90 rounded-2xl shadow-2xl p-10 border border-gray-800">
    <h1 class="text-3xl font-bold text-center text-white mb-2">Enquetes que Votei</h1>
    <p class="text-center text-gray-400 mb-8">Confira as enquetes que você participou.</p>

    <% if @polls.any? %>
      <div class="space-y-4">
        <% @polls.each do |poll| %>
          <div class="bg-gray-800 bg-opacity-70 rounded-lg p-6 shadow-xl border border-gray-700 hover:bg-gray-700 transition">
            <h3 class="text-lg font-medium text-white"><%= poll.question %></h3>
            <p class="text-sm text-gray-400 mt-1">
              Criada por: <%= poll.user.email %> •
              Status: <span class="font-semibold"><%= poll.status == 'open' ? '🟢 Aberta' : '🔴 Fechada' %></span>
            </p>
            <p class="text-sm text-gray-400 mt-1">
              Seu voto: <span class="font-semibold text-blue-400"><%= current_user.votes.find_by(poll: poll).option.content %></span>
            </p>
            <%= link_to 'Ver Resultados', poll, class: 'mt-3 inline-block text-blue-400 hover:text-blue-300 font-semibold' %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <p class="text-gray-400">Você ainda não votou em nenhuma enquete.</p>
        <%= link_to 'Explorar Enquetes', root_path, class: 'text-blue-400 hover:text-blue-300 mt-2 inline-block' %>
      </div>
    <% end %>
  </div>
</div>